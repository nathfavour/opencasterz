class Device {
  final String id;
  final String name;
  final String type;

  Device({
    required this.id,
    required this.name,
    required this.type,
  });

  // TODO: Add JSON serialization if needed
}
